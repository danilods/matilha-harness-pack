# Wave 5c Smoke Test Results

**Date:** 2026-04-20
**Pack:** matilha-harness-pack on branch `wave-5c-harness-pack`
**matilha CLI tests:** 903/903 passing (743 baseline post-Wave 5b + 160 new harness-pack validation)
**Platform:** macOS (local dev). Claude Code marketplace install deferred to post-ship (replicates Wave 5a/5b precedent — actual `/plugin install` requires either marketplace publication or `/plugin marketplace add` registration of the local repo first).

## Structural smoke (automated via matilha CLI validator)

| Artifact | Status | Notes |
|---|---|---|
| `.claude-plugin/plugin.json` | ✅ | name=matilha-harness-pack; version=0.1.0; matilha-pack keyword present |
| `.claude-plugin/marketplace.json` | ✅ | valid JSON; pluginref category=harness |
| `CLAUDE.md` + `GEMINI.md` + `AGENTS.md` + `README.md` | ✅ | all contain slogan "You lead. Agents hunt." |
| `LICENSE` | ✅ | MIT (auto-generated by gh repo create) |
| `docs/wiki-ingestion-workflow.md` | ✅ | 5-step discipline documented, harness-specific example |
| 22 skills canonical | ✅ | all `harness-*`, 13 sections, Sources wikilinks, line counts 171-220, descriptions starting with "Use when" |
| Activation uniqueness heuristic | ✅ | passes (no pair > 80% description word overlap) |
| Overlap disclosure (2 skills) | ✅ | `harness-eval-roadmap-0-to-1` and `harness-context-rot-budget` both contain "Complements" phrase verbatim |
| Paraphrase discipline (humano grep) | ✅ | "code is free" only in `harness-code-is-free` (concept skill) + 1 attributed quote in `harness-architecture`; "token billionaire" 0 occurrences; "Continue typed" only as attributed quotes; "Ralph Wiggum" only in `harness-ralph-wiggum-loop` (concept skill); "GAN-inspired" used as standard ML descriptor; no Anthropic narrative-voice copy detected |

## Validation suite (matilha CLI repo)

- `cd /Users/danilodesousa/Documents/Projetos/matilha && npm test` → **903/903 green** (743 baseline + 160 new harness-pack validation tests)
- 160 new tests broken down:
  - 3 plugin.json tests
  - 88 frontmatter tests (22 × 4)
  - 66 body tests (22 × 3)
  - 1 activation-uniqueness heuristic
  - 2 overlap-disclosure tests

## Semantic smoke (mental-model walkthrough)

Three representative cluster scenarios. Predicted activations are based on reading each skill's `description` against the user intent — Skill tool fuzzy-matches intent to descriptions, so multi-skill activation is the norm.

### Scenario 1: User designs multi-agent system
- **Intent:** "I'm building a 4-week MVP that needs to run autonomously over the weekend. How do I structure the agents?"
- **Predicted primary activation:** `harness-architecture` — description: "Use when designing multi-agent system for long-running tasks — Planner/Generator/Evaluator + sprint contract architecture."
- **Predicted secondary:** `harness-workflow-vs-agent-decision` (decide pattern) + `harness-evolution` (re-audit harness as model evolves over the build).
- **Core delegation:** `matilha-plan` Phase 30 via Companion Integration block.
- **Verdict:** ✅ matches expected harness cluster

### Scenario 2: User hits context window
- **Intent:** "My research agent runs out of context after 3 hours. What should I do?"
- **Predicted primary activation:** `harness-long-horizon-strategies` — description: "Use when task exceeds context window — choose between compaction, structured note-taking, or sub-agent architecture."
- **Predicted secondary:** `harness-jit-retrieval` (alternative/complement: maybe agent loads too much upfront), `harness-context-rot-budget` (audit prompt size).
- **Core delegation:** `matilha-hunt` Phase 40 via Companion Integration block.
- **Verdict:** ✅ matches expected harness cluster

### Scenario 3: User scales team review process
- **Intent:** "My team keeps getting the same review comments on PRs every week. How do I systematize this?"
- **Predicted primary activation:** `harness-garbage-collection-day` — description: "Use when onboarding team ritual — weekly ritual converting recurring human PR feedback into doc + persistent review agent."
- **Predicted secondary:** `harness-nfrs-as-prompts` (where to put the encoded NFR), `harness-review-agents-by-persona` (route the recurring slop to a persona-specialized agent).
- **Core delegation:** Phase 70 onboarding via Companion Integration block.
- **Verdict:** ✅ matches expected operational cluster

### Scenario 4: Cross-pack dual activation (overlap surfaces)
- **Intent:** "I'm designing the eval suite for my coding agent. What's the defense-in-depth model?"
- **Predicted DUAL activation:** `harness-eval-roadmap-0-to-1` (agent-eval Swiss Cheese 6 layers) + `matilha-ux-pack:ux-swiss-cheese-errors` (Reason's Swiss Cheese applied to human-error tolerance).
- **Companion Integration phrase verifies cross-pack discoverability:** `Complements matilha-ux-pack:ux-swiss-cheese-errors at agent-eval-defense vs human-error-tolerance` — present in body.
- **Verdict:** ✅ overlap discipline produces complementary coverage rather than collision

## Companion Integration coverage (cross-pack)

When all 3 packs (matilha-skills core + matilha-ux-pack + matilha-growth-pack + matilha-harness-pack) are installed, the activation graph routes naturally:

| User question type | Routes to |
|---|---|
| "How do I gamify onboarding ethically?" | `growth-octalysis-drives` (strategic) + `ux-intrinsic-motivation` (feature-level) |
| "How do I form user habits without dark patterns?" | `growth-hook-model-product` (product flow) + `ux-variable-rewards` (feature mechanic) |
| "How do I make this signup form less painful?" | `cog-cognitive-load` (working memory) — and now overlap-aware against `harness-context-rot-budget` (LLM attention budget) |
| "How do I design evals for my agent + handle failure modes for users?" | `harness-eval-roadmap-0-to-1` (agent-eval Swiss Cheese) + `ux-swiss-cheese-errors` (human-error Swiss Cheese) |

The 2 explicit overlap disclosures in this pack cement the pattern: same model, complementary application.

## Overall verdict

✅ **Ship-ready.** All structural validation gates pass; semantic walkthrough confirms expected activations align with skill descriptions; overlap discipline produces cross-pack value rather than collision. Validator suite acts as the regression net for any future drift.

## Validation snapshot

```
$ cd ~/Documents/Projetos/matilha && npm test 2>&1 | tail -5

 Test Files  65 passed (65)
      Tests  903 passed (903)
   Start at  21:10:05
   Duration  6.09s
```

## Out-of-band notes

Actual `/plugin install` requires marketplace registration. Two paths:
1. **Local dev install** (post-ship): `/plugin marketplace add /Users/danilodesousa/Documents/Projetos/matilha-harness-pack` then `/plugin install matilha-harness-pack@matilha-harness-pack`.
2. **Marketplace publication** (post-ship): submit pack to Anthropic's official Claude Code plugin marketplace; users install via `/plugin install matilha-harness-pack`.

Per Wave 5a/5b precedent, structural + semantic smoke is sufficient for tag and ship; live activation testing happens on first user adoption (which is when real-world Skill tool routing becomes observable).
